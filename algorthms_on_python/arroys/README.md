## Array algorithms

Implemented:

* `quicksort_hoare.py` — QuickSort with Hoare partition scheme
* `merge_sort.py` — MergeSort (divide and conquer)
* `sorts_n2.py` — O(n²) sorts (educational baseline)

Each module contains:

* complexity analysis (time/space)
* a minimal runnable example in `__main__`

## String algorithms

Implemented:

* `substring_search.py` — naive substring search (all occurrences)
* `equal.py` — helper for equality check (educational)

Each module contains:

* complexity analysis (time/space)
* a minimal runnable example in `__main__`
